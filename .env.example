# Copy to .env and adjust as needed
# Backend URL for server-side product filtering
VITE_PRODUCTS_API_URL=http://localhost:5178

# Image source preference: 'unsplash' (default) or 'picsum'
VITE_IMAGE_SOURCE=unsplash
